# Self-Heal Runtime

1. Inspect the failing surface: binary, PATH, config, MCP, agents, skills, or wrappers.
2. Reproduce the issue with the smallest useful command.
3. Patch the runtime artifact with the smallest safe change.
4. Re-run the doctor or verification command.
5. Record the fix as a durable lesson only after approval.
